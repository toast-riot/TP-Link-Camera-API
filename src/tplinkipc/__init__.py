from .api import TPLinkIPC

# NOTE: I've prefixed some comments with "original:" to indicate the equivalent method or value's name in
# the original code of the camera, so you can look there for more context